syntax "next-router/0.1";

# PPIO (OpenAI-compatible)
provider "ppio" {
  defaults {
    upstream_config {
      base_url = "https://api.ppio.com/openai";
    }
    auth {
      auth_bearer;
    }
    error {
      error_map openai;
    }
    response {
      resp_passthrough;
    }
    models {
      models_mode openai;
    }
  }

  match api = "chat.completions" stream = false {
    metrics {
      usage_extract openai_chat_completions;
      finish_reason_extract openai_chat_completions;
    }
    upstream {
      set_path "/v1/chat/completions";
    }
  }

  match api = "chat.completions" stream = true {
    metrics {
      usage_extract openai_chat_completions;
      finish_reason_extract openai_chat_completions;
    }
    request {
      # PPIO supports stream_options.include_usage for stream usage stats.
      json_set "$.stream_options.include_usage" true;
    }
    upstream {
      set_path "/v1/chat/completions";
    }
  }

  match api = "completions" {
    metrics {
      usage_extract openai_prompt_completion;
      finish_reason_extract openai_completions;
    }
    upstream {
      set_path "/v1/completions";
    }
  }

  match api = "embeddings" {
    metrics {
      usage_extract openai_embeddings;
    }
    upstream {
      set_path "/v1/embeddings";
    }
  }
}
